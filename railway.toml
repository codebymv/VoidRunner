[build]
  command = "npm install && npm run build"

[deploy]
  startCommand = "npx serve -s dist -p $PORT"
  healthcheckPath = "/"
  restartPolicyType = "ON_FAILURE"

[deploy.envs]
  NODE_ENV = "production"